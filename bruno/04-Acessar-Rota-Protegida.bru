meta {
  name: 04-Acessar-Rota-Protegida
  type: http
  seq: 4
}

get {
  url: {{order_url}}/api/orders
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 200
  res.body: isArray
}
